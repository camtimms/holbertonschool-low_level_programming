#include "main.h"
/**
 * _abs - calculates the absolute value for a number
 * @n: int
 * Description: calculates the absolute value for a number
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
